# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and aqcuire TLS certificates
DOMAIN=api.barrels.gd
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=https://dashboard.api.barrels.gd
# In staging and production, set this env var to the frontend host, e.g.
# FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Barrels API"
STACK_NAME=barrels-api

# Backend
BACKEND_CORS_ORIGINS="https://dashboard.api.barrels.gd,https://api.barrels.gd"
SECRET_KEY=PYntVxuXMFB5_WDqyumpT6Sz5TQA18LvC60CGT3UMxk
FIRST_SUPERUSER=admin@barrels.gd
FIRST_SUPERUSER_PASSWORD=Piettet0p1
SMOKESHOW_AUTH_KEY='sgCAkCY2O/+QnzE6JTN4uvmht7DT6u5/yKMMMtG6N+r5y9vPy67oacFp2GKuLquGN1w'

# Emails
SMTP_HOST=smtp.postmarkapp.com
SMTP_USER=barrelsgd
SMTP_PASSWORD=Piette0p1
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Piettet0p1

SENTRY_DSN=1

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
