name: Deploy to Production

on:
  release:
    types:
      - published

jobs:
  deploy:
    # Do not deploy in the main repository, only in user projects
    if: github.repository_owner != 'fastapi'
    runs-on:
      - self-hosted
      - production
    environment:
      name: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup environment
        run: |
          # Normalize .env newlines to avoid sed/crlf issues
          [ -f .env ] || cp .env.example .env
          perl -pi -e 's/\r$//;' .env || true
          # Preserve existing .env if it exists, then enforce prod values
          sed -i "s/^ENVIRONMENT=.*/ENVIRONMENT=production/" .env
          sed -i "s/^DOMAIN=.*/DOMAIN=api.barrels.gd/" .env
          if grep -q '^STACK_NAME=' .env; then sed -i "s/^STACK_NAME=.*/STACK_NAME=barrels-api/" .env; else echo "STACK_NAME=barrels-api" >> .env; fi
          if grep -q '^PROJECT_NAME=' .env; then sed -i "s/^PROJECT_NAME=.*/PROJECT_NAME=barrels-api/" .env; else echo "PROJECT_NAME=barrels-api" >> .env; fi
          # FRONTEND public origin for emails and links
          if grep -q '^FRONTEND_HOST=' .env; then sed -i "s|^FRONTEND_HOST=.*|FRONTEND_HOST=https://api.barrels.gd|" .env; else echo "FRONTEND_HOST=https://api.barrels.gd" >> .env; fi
          # Distinct logical DB name for production (optional)
          if grep -q '^POSTGRES_DB=' .env; then sed -i "s/^POSTGRES_DB=.*/POSTGRES_DB=app_prod/" .env; else echo "POSTGRES_DB=app_prod" >> .env; fi
          # SENTRY must be empty unless valid DSN; force empty to avoid legacy '1'
          sed -i 's/^SENTRY_DSN=.*/SENTRY_DSN=/' .env
          # Expose PROJECT_NAME to subsequent steps
          echo "PROJECT_NAME=$(grep '^PROJECT_NAME=' .env | cut -d'=' -f2 | tr -d '\r')" >> $GITHUB_ENV
          # Debug echo to confirm consolidation
          echo "=== Effective .env (sanitized) ==="; sed -n '1,200p' .env | sed -E 's/(SECRET_KEY|POSTGRES_PASSWORD|FIRST_SUPERUSER_PASSWORD)=.*/\1=****/'
          echo "Using PROJECT_NAME=$PROJECT_NAME"
          
      - name: Ensure FIRST_SUPERUSER_PASSWORD length <= 40
        run: |
          set -euo pipefail
          cur=$(grep -E '^FIRST_SUPERUSER_PASSWORD=' .env | cut -d'=' -f2- | tr -d '\r' || true)
          if [ -z "${cur:-}" ]; then
            new=$(openssl rand -base64 48 | tr -d '=\n' | cut -c1-32)
            sed -i "s/^FIRST_SUPERUSER_PASSWORD=.*/FIRST_SUPERUSER_PASSWORD=${new}/" .env
            echo "Set FIRST_SUPERUSER_PASSWORD to random 32-char value"
          else
            len=${#cur}
            if [ "$len" -gt 40 ]; then
              new=$(echo "$cur" | cut -c1-40)
              sed -i "s/^FIRST_SUPERUSER_PASSWORD=.*/FIRST_SUPERUSER_PASSWORD=${new}/" .env
              echo "Truncated FIRST_SUPERUSER_PASSWORD from $len to 40 characters"
            else
              echo "FIRST_SUPERUSER_PASSWORD length OK ($len)"
            fi
          fi
          
      - name: Retire legacy eugine-api stack (safety)
        run: |
          docker compose -f docker-compose.yml -p eugine-api down || true

      - name: Build with Docker Compose
        run: |
          set -euxo pipefail
          echo "Using: docker compose --env-file .env -f docker-compose.yml -p 'barrels-api' build"
          docker compose --env-file .env -f docker-compose.yml -p "barrels-api" build
          
      - name: Deploy with Docker Compose
        run: |
          set -euxo pipefail
          echo "Using: docker compose --env-file .env -f docker-compose.yml -p 'barrels-api' up -d ..."
          echo "Starting database..."
          docker compose --env-file .env -f docker-compose.yml -p "barrels-api" up -d db
          echo "Waiting for database to be ready..."
          for i in {1..20}; do
            status=$(docker inspect --format='{{json .State.Health.Status}}' "barrels-api-db-1" 2>/dev/null || echo null)
            if echo "$status" | grep -q healthy; then echo "Database is ready"; break; fi
            echo "Waiting for database to be ready... ($i/20)"; sleep 3
            if [ $i -eq 20 ]; then
              echo "::error::Database did not become ready in time";
              docker compose -p "barrels-api" logs db || true
              exit 1
            fi
          done

          echo "Starting all services..."
          set +e
          docker compose --env-file .env -f docker-compose.yml -p "barrels-api" up -d --remove-orphans
          up_status=$?
          echo "=== Service status ==="; docker compose -p "barrels-api" ps || true

          echo -e "\n=== Prestart logs (tail 120) ==="
          docker compose --env-file .env -p "barrels-api" logs prestart --tail=120 2>&1 || true

          echo -e "\n=== Backend logs (tail 120) ==="
          docker compose --env-file .env -p "barrels-api" logs backend --tail=120 2>&1 || true

          echo -e "\n=== DB logs (tail 60) ==="
          docker compose --env-file .env -p "barrels-api" logs db --tail=60 2>&1 || true

          echo -e "\n=== Traefik logs (tail 60) ==="
          docker compose --env-file .env -p "barrels-api" logs traefik --tail=60 2>&1 || true

          echo -e "\n=== Effective .env (sanitized) ==="
          sed -n '1,200p' .env | sed -E 's/(SECRET_KEY|POSTGRES_PASSWORD|FIRST_SUPERUSER_PASSWORD)=.*/\1=****/' || true

          if [ $up_status -ne 0 ]; then
            echo "::error::docker compose up failed with status $up_status";
            exit $up_status
          fi

          # Quick external health check; do not fail the job, just print result
          set -e
          echo -e "\n=== External health check ==="
          curl -sS https://api.barrels.gd/api/v1/utils/health-check/ || echo "health-check request failed"

      - name: Guard: ensure barrels-api (not eugine-api)
        run: |
          set -euxo pipefail
          echo "Checking for unintended eugine-api containers..."
          if docker compose -p eugine-api ps | grep -q "eugine-api-"; then
            echo "::error::Detected eugine-api containers running; the workflow likely missed -p barrels-api in a step."
            echo "=== compose ps (eugine-api) ==="
            docker compose -p eugine-api ps || true
            echo "Shutting down legacy stack to prevent drift..."
            docker compose -f docker-compose.yml -p eugine-api down || true
            exit 1
          else
            echo "OK: No eugine-api containers running."
          fi

      - name: Show logs on failure
        if: failure()
        run: |
          echo "=== docker ps ===" && docker ps -a || true
          echo "=== compose ps (barrels-api) ===" && docker compose -p "barrels-api" ps || true
          PRESTART_ID=$(docker compose -p "barrels-api" ps -q prestart || true)
          BACKEND_ID=$(docker compose -p "barrels-api" ps -q backend || true)
          DB_ID=$(docker compose -p "barrels-api" ps -q db || true)
          echo "Prestart container: ${PRESTART_ID:-none}"; echo "Backend container: ${BACKEND_ID:-none}"; echo "DB container: ${DB_ID:-none}"
          if [ -n "$PRESTART_ID" ]; then
            echo "=== prestart inspect ==="; docker inspect "$PRESTART_ID" --format 'ExitCode={{.State.ExitCode}} StartedAt={{.State.StartedAt}} FinishedAt={{.State.FinishedAt}} Error={{.State.Error}}' || true
          fi
          echo "=== prestart logs (tail 400) ===" && docker compose --env-file .env -f docker-compose.yml -p "barrels-api" logs --no-color --tail=400 prestart || true
          echo "=== prestart suspected errors ===" && docker compose --env-file .env -f docker-compose.yml -p "barrels-api" logs --no-color prestart | egrep -i "(pydantic|validation|sentry|dsn|password|FIRST_SUPERUSER|alembic|traceback|exception)" || true
          echo "=== db logs (tail 200) ===" && docker compose --env-file .env -f docker-compose.yml -p "barrels-api" logs --no-color --tail=200 db || true
          echo "=== backend logs (tail 200) ===" && docker compose --env-file .env -f docker-compose.yml -p "barrels-api" logs --no-color --tail=200 backend || true
          echo "=== env (sanitized) ===" && sed -n '1,200p' .env | sed -E 's/(SECRET_KEY|POSTGRES_PASSWORD|FIRST_SUPERUSER_PASSWORD)=.*/\1=****/' || true
          echo "=== health check (best effort) ==="; curl -sS https://api.barrels.gd/api/v1/utils/health-check/ || true
